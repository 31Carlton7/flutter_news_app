import 'package:canton_design_system/canton_design_system.dart';

class CategoryListViewHeader extends StatelessWidget {
  const CategoryListViewHeader({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return ViewHeaderTwo(
      title: 'Categories',
    );
  }
}
